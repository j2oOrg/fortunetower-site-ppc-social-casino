apiVersion: apps/v1
kind: Deployment
metadata:
  name: fortunetower-site-ppc-social
  labels:
    app: fortunetower-site-ppc-social
spec:
  replicas: 1
  selector:
    matchLabels:
      app: fortunetower-site-ppc-social
  template:
    metadata:
      labels:
        app: fortunetower-site-ppc-social
    spec:
      containers:
        - name: fortunetower-site-ppc-social
          image: IMAGE_PLACEHOLDER
          ports:
            - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: fortunetower-site-ppc-social
  labels:
    app: fortunetower-site-ppc-social
spec:
  type: LoadBalancer
  selector:
    app: fortunetower-site-ppc-social
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
  loadBalancerIP: LOAD_BALANCER_IP_PLACEHOLDER
